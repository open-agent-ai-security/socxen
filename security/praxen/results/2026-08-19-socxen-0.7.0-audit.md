# socxen findings adjudication — 2026-08-19T02:14:10Z

Auditor pass over `reports/socxen-findings-2026-08-19.json` (14 findings) against
`scan-high/WORKER_REMIT.md` and the workspace at
`/Users/steve.wilson/Documents/github/deckard/local/socxen-redaction/src`.
All line numbers below are lines I read in the workspace, not lines quoted from the findings file.

## Audit verdicts

### PRAX-2026-08-19-001 — CONFIRMED

Read: `plugin/install.sh:34-39` (flag parsing, `MERGE_PERMS=0` default), `:485-527` (gate evaluation and
the merge branches), `plugin/docs/installation.md:29-33` (marketplace install), `:118-136` (Governance
section), `plugin/.claude-plugin/plugin.json` (whole file), `plugin/skills/soc-investigate/settings.snippet.json`
(tier sizes: allow 18, ask 4, deny 68).

Every load-bearing element holds: line 509 is exactly the `elif [ -n "$BLOCKER" ] || [ "$ASSUME_YES" = 1 ] || [ ! -t 0 ]`
warn-only branch, the interactive offer at 515-525 reads `[y/N]` and treats anything but y/yes as decline, and
the comment at 511-512 states installation alone must never change `settings.json`. `plugin.json` declares only
`skills`, with no hooks or postinstall, so the documented primary path (`claude plugin install socxen@open-agent-ai-security`,
installation.md:33) genuinely never evaluates the gate. Minor citation drift only: the quoted
"Until it's merged there is no permission-layer gate" sits at installation.md:132, not :129, and the marketplace
command at :33 rather than :30-32 — same blocks, no change to the claim.

### PRAX-2026-08-19-002 — CONFIRMED

Read: `plugin/skills/soc-investigate/SKILL.md:106-127` and `:180`, plus a grep of that file for
`permission|harness|settings.json|merged`.

The two quotations are verbatim at 115-117 and 124-125, and the third instance at :180 repeats the inversion
("your ask is the lock — the permission prompt can be bypassed"). The grep returns no runtime read of the gate's
state anywhere in the skill body, so the "no disclosure of whether the pack is merged" half also holds. The
installer's `gate_on()` at install.sh:434 is the counterpart the finding says exists and is not reused, which
I confirmed.

### PRAX-2026-08-19-003 — CONFIRMED

Read: `plugin/connector/canonicalize.py:41-78` (the `Hygiene` dataclass at 50-56 and the strip loop at 66-74),
`grep -n flag` across `canonicalize.py` and `tests/test_canonicalize.py` (zero hits in both),
`security/design/input-canonicalizer.md:8-32`.

`Hygiene` carries `removed` and `counts` only; the loop appends to `removed` for `_LINE_SEP` and `_STRIP`
members alone, so kept ZWJ/ZWNJ/LRM/RLM/ALM leave no record. The design document's authoritative
"What's built vs. deferred (authoritative — PR #32)" block does promise both "Flag `obfuscated-ascii`" and a
hygiene record of "`removed` (code points) + `flagged` + `counts`". Citation drift only: those two promises are
at :20-24 rather than :14 (:14 is the Strip bullet of the same authoritative block).

### PRAX-2026-08-19-004 — CONFIRMED

Read: `plugin/connector/exabeam-mcp-bridge.py:110-115` (`WRITE_TOOLS`, `_DEFANG_FIELDS`), `:227-229`
(`list_tools`), `:232-262` (`call_tool`), `plugin/skills/soc-investigate/reference/tool-map.md:20-45`.

Both citations are exact. `list_tools` is a one-line passthrough of `(await remote(...)).tools` with no filter,
no pin and no screening — and `_canon_content` is applied only to `call_tool` results (`:176-177`), never to the
tool inventory, so upstream-authored tool descriptions do reach model context unscreened. `call_tool` dispatches
on whatever `name` arrives, with `is_write = name in WRITE_TOOLS` as the only name-keyed branch. R-18's
`rule_text` is a list heading, but the section it heads is a genuine allowlist (read tools, four write tools,
audit tap), so the closure is real and unenforced.

### PRAX-2026-08-19-005 — CONFIRMED (with note)

Read: `plugin/connector/exabeam-mcp-bridge.py:111-115`, `:178-202` (`_defang_value`, `_defang_args`),
`plugin/skills/soc-investigate/reference/tool-map.md:36-40` (the write-tool field surface).

The mechanism claim is exactly as cited: `_defang_args` at 192-202 neutralizes a value only when
`k.lower() in _DEFANG_FIELDS`, recurses otherwise, and returns scalars untouched, so reach is defined by two
literal sets with no signal at their edge. Note for the reader: the illustrative example ("a plain description,
comment, summary or title on one of those same four tools") has no instance in the target's documented surface —
tool-map.md:37-40 enumerates the free-text fields of the four write tools as exactly `note`, `closedReason`,
`supportingReason`, `alertDescription`, `alertName`, `tags`, which is precisely `_DEFANG_FIELDS`. The residual
is therefore prospective (a remote-added or renamed tool or field, i.e. the coupling to -004) rather than
present-tense on today's schema. The cited evidence supports the finding as written, so this is not a kill.

### PRAX-2026-08-19-006 — CONFIRMED

Read: `plugin/docs/installation.md:93-113` (the `<details>` "Advanced — wire it manually (no auto-refresh)"
block at 97-109), `plugin/docs/security-guardrails.md:8-28` and `:63-74`.

The Advanced block registers the remote MCP directly with `claude mcp add --transport http exabeam …`, bypassing
the bundled bridge, and names only ~4h token expiry as the cost. security-guardrails.md:15-16 reads verbatim
"socxen runs two automatic checks on every Exabeam call. They're always on and need no configuration", and :72
adds "This applies to **every** link socxen writes" — both true only under the bundled registration, since
`canonicalize`, `neutralize_output` and the observra tap all live in the bridge process.

### PRAX-2026-08-19-007 — CONFIRMED

Read: `plugin/skills/triage-cases/SKILL.md:165-172`, `plugin/skills/rule-tuning/SKILL.md:90-96` and `:175-179`,
`security/praxen/WORKER_REMIT.md:150-151` and `:198`.

Citations are exact: "save an oversized result and parse it from file" at triage-cases:170-171, "expect to save
and parse the result, not read it inline" at rule-tuning:94, "parse them from file" at rule-tuning:179. The
instruction necessarily implies a harness filesystem write of raw tenant case and rule content. I checked
whether R-21/R-31 might be scan-invented obligations that criminalize an intended feature: they are not — the
maintainers' own in-repo remit carries both clauses verbatim (`security/praxen/WORKER_REMIT.md:150-151`, `:198`),
and the Known Good Baseline names only the credential file (read) and the audit log (append) as file paths.

### PRAX-2026-08-19-008 — CONFIRMED

Read: `plugin/connector/neutralize_output.py:20-32` (DOCUMENTED RESIDUALS block),
`tests/test_neutralize_output.py:50-62`, `plugin/docs/security-guardrails.md:40-55`, `:63-74`, `:78-101`,
and the remit's Declared Redaction Limits (WORKER_REMIT.md:248-263).

The residual is stated at neutralize_output.py:26-27 and asserted as intended by
`test_bare_urls_in_prose_untouched` at tests/test_neutralize_output.py:61 — both exactly as cited. The
countervailing doc claim is at security-guardrails.md:49-50 ("Any link written into a note is **escaped**")
and is reinforced at :72 ("This applies to **every** link socxen writes"), while the residual acknowledgement
sits far below at :100-101. Citation drift only: the "Clickable links" bullet is at :49, not :47. The remit's
Declared Redaction Limits list does not include prose URLs, so the residual is genuinely undeclared at policy
level — which is what the finding says.

### PRAX-2026-08-19-009 — CONFIRMED (with note)

Read: `plugin/connector/exabeam-mcp-bridge.py:156-175`, `plugin/connector/observra_logging.py:60-72` and
`:145-156`, `security/design/input-canonicalizer.md:8`, `:10`, `:161-166`.

The load-bearing evidence holds exactly: the `_canon_content` except branch at :171-172 writes
"bridge: canonicalize passthrough after error" to stderr and appends the unmodified block, with nothing marking
the result; `_disable()` at observra_logging.py:67-71 and the dropped-event write at :155 use the same channel.
Note on one sub-clause: the failopen counter and per-block `status` marker are promised at
`security/design/input-canonicalizer.md:164`, not `:10`, and :164 sits in "§10. Fail-open & performance", which
the document's own status line (:8) classes as intent ("the fuller design is intent") rather than in the
authoritative "What's built" block. That weakens only the spec-promise aside; the primary claim — that the
disclosures land on a stdio subprocess's stderr rather than anywhere the analyst sees — stands, and the finding
already carries Medium confidence and log_only escalation.

### PRAX-2026-08-19-010 — CONFIRMED

Read: `plugin/connector/observra_logging.py:86-132`, `plugin/docs/logging.md:88-100`.

Exact: `kwargs["path"]` is assigned only inside the `if backend == "jsonl"` branch at :92-105, and the
disclosure at :127-128 computes `where = kwargs.get("path", backend)` before printing
"bridge: structured logging on -> {where}". For `exabeam`, `otel`, `otel_log` and `webhook`, `where` is the
backend word. logging.md:95 promises verbatim "the bridge prints one line to stderr naming the destination and
how to turn it off — disclosed, not silent". R-11's "disabled by default" half is satisfied (jsonl is the
default); the divergence is the disclosure half only, which is how the finding frames it.

### PRAX-2026-08-19-011 — CONFIRMED (with note)

Read: `plugin/.mcp.json` (whole file, 9 lines), `.github/workflows/ci.yml:12-32` plus a grep of `uses:` across
all workflows, `plugin/connector/exabeam-mcp-bridge.py.lock:1-3`.

`args` at .mcp.json:6 is `["run", "--quiet", "${CLAUDE_PLUGIN_ROOT}/connector/exabeam-mcp-bridge.py"]` with no
`--locked`; `actions/checkout@v4` at ci.yml:18 and `astral-sh/setup-uv@v5` at :21 are mutable tags, and
`invariants` is the workflow's only job, so there is indeed no scanning job. Note: R-24's literal clause
("MUST be hash-pinned in the shipped lockfile") is satisfied — the lockfile ships and pins — so the divergence
rests entirely on the rule's purpose clause ("so a fresh install resolves the same dependency tree"), which is a
fair reading given `uv run` re-resolves rather than fails on a lock that no longer satisfies the PEP 723 header.

### PRAX-2026-08-19-012 — CONFIRMED

Read: `plugin/install.sh:108-120`, `plugin/connector/exabeam-mcp-bridge.py:40-62`.

Exact: the mode is read at install.sh:115 via `stat` and line 116 emits
`warn "  $ENV_FILE is mode $perms — consider: chmod 600 $ENV_FILE"` with nothing that tightens it; `load_env()`
at bridge:43-54 parses the dotfile as plaintext `key=value` with no keychain or vault path, and `CFG`/`KEY`/
`SECRET` are bound at :57-60. The finding correctly labels this posture rather than divergence and carries
`policy_rule_ids: null`, so no rule status is at stake.

### PRAX-2026-08-19-013 — CONFIRMED

Read: `plugin/docs/installation.md:14-20`, `plugin/skills/soc-investigate/SKILL.md:1-12` (frontmatter) and
`:26-36` (Preflight), plus a tree-wide grep for `Sonnet 4.6|sonnet-4-6`.

The floor and the Haiku exclusion are stated at installation.md:16-19 exactly. The skill frontmatter declares
`name` and `description` only, with no model field, and the Preflight section at :29-36 verifies only that the
`exabeam_*` tools are reachable. The grep's other hits are CHANGELOG, the red-team plan/methodology/results,
the eval and red-team runners, the in-repo Praxen artifacts and the guardrail doc — no runtime surface, which
is the claim.

### PRAX-2026-08-19-014 — CONFIRMED

Read: `plugin/connector/neutralize_output.py:362-390`.

The docstring at 365-366 asserts "Redaction runs FIRST, so a secret is masked before any structural transform
can split or requote it", while the comment beginning at :371 states "ORDER MATTERS. Link defang runs BEFORE
redaction, not after" and the implementation applies `_MD_LINK_RE.sub(_link, text)` at :386 before
`redact_secrets` at :388 and `_neutralize_formulas` at :390. Behavior is correct and the docstring is wrong,
exactly as described. Citation drift only: the code the second snippet points at runs to :390, not :385.

## Remit feedback

(none)

Four rules were examined closely as candidates and none qualified:

- **R-18** (`"Allowed Tools (Known Good Baseline)"`) is a heading-as-rule, but the section it heads is a real
  allowlist — Exabeam read tools, the four non-destructive write tools, the local audit tap — not definitional
  prose, so the closure it creates is a genuine obligation. Its categorical read-tool phrasing covers every tool
  in `plugin/skills/soc-investigate/reference/tool-map.md:20-60`.
- **R-21 / R-31** prohibit exactly the save-to-file behavior two shipped skill bodies instruct, which superficially
  looks like "prohibits a documented feature". It is not remit over-reach: the maintainers' own in-repo remit
  carries both clauses verbatim (`security/praxen/WORKER_REMIT.md:150-151`, `:198`) and the Known Good Baseline
  lists only the credential file and the audit log as file paths, so the divergence is in the skill prompt, not
  in the rule.
- **R-42 / R-63** are read by findings -003 and -009 as requiring disclosure the analyst can actually see, while
  `plugin/docs/logging.md:95` documents stderr as the intended disclosure channel and
  `security/design/input-canonicalizer.md:164` documents a stderr diagnostic for the fail-open. Neither rule names
  a channel, and neither demands a mechanism the docs never contemplated — the design spec's authoritative block
  (`:20-24`) commits to the `flagged` record R-42 asks for — so the channel question is finding adjudication, not
  a rule defect.
- **R-47** is supported by the target's own headline claim (`plugin/docs/security-guardrails.md:49-50`, `:72`),
  even though the same document records the prose-URL residual at `:100-101`; the contradiction is internal to the
  target's docs, which is what finding -008 reports.

## Audit summary

- CONFIRMED: 14
- UNSUPPORTED: 0
- REMIT-DEFECT: 0
- Remit-feedback entries: 0

Every finding's cited artifacts exist where claimed, and every load-bearing claim survived an attempt to refute
it against the code. Citation quality is high: eleven of fourteen findings cite exact lines, and the three drifts
I found (installation.md :129→:132 and :30-32→:33 in -001; input-canonicalizer.md :14→:20-24 in -003 and :10→:164
in -009; security-guardrails.md :47→:49 in -008) all land inside the same block and change nothing about the
claims. Three findings carry notes rather than kills: -005's illustrative uncovered field has no instance in the
documented write-tool schema, so its residual is prospective rather than present-tense; -009's appeal to a
design-spec promise points at a section the spec itself labels intent; and -011's rule is literally satisfied by
the shipped lockfile, with the divergence resting on the rule's purpose clause. None of these disturb the
underlying mechanism claims, each of which I verified in source. On the rule side, the four rules most exposed to
over-reach — the tool-list closure, the filesystem prohibition pair, the disclosure pair, and the link-defang
clause — all trace to obligations the target's own remit or documentation states, so no rule needs narrowing on
this pass.

## Cleanup record

**No cleanup was required.** All 14 findings returned CONFIRMED; there were no UNSUPPORTED and no
REMIT-DEFECT verdicts, and the rule-level remit check produced no feedback entries. Under high mode's
Phase 3, that means the Phase-1 artifacts are the final deliverable **unchanged**: no finding was
removed, no `related_findings` entry was stripped, no rule status was re-stated, no RAISE category
score was re-derived, and the `weighted_overall` of 3.15 stands as originally computed. There is
therefore no `-raw` artifact pair — the single artifact set below is both the raw and the final report.

Final artifacts (unchanged from Phase 1):
- reports/socxen-findings-2026-08-19.json
- reports/socxen-analysis-2026-08-19-021410.html
- reports/socxen-analysis-2026-08-19-021410.txt
- reports/socxen-draft-2026-08-19-021410.md (Step 9.9 manifest)
- reports/socxen-evidence-2026-08-19-021410.txt (Step 4 checkpoint, RAISE NOTES, MATURITY M1-M12, THEMES)

**Notes carried forward from the audit (recorded, not acted on).** Three CONFIRMED verdicts carried
qualifying notes and the auditor recorded four citation offsets. The orchestrator re-read the four
cited locations and agrees with the auditor: in every case the quoted text is verbatim and present, and
the cited line falls inside or immediately adjacent to the correct block, so no claim is affected and no
re-render was warranted. For a future run's accuracy, the precise offsets are:
- PRAX-2026-08-19-001 — installation.md quoted sentence is at :132 (cited :129); the marketplace
  install command is at :33 (cited :30-32).
- PRAX-2026-08-19-003 — the design spec's "Flag obfuscated-ascii" and "removed + flagged + counts"
  bullets are at :20-24 (cited :14, the section's first bullet).
- PRAX-2026-08-19-008 — the "Any link written into a note is escaped" claim is at :49 (cited :47).
- PRAX-2026-08-19-005 — the auditor notes the uncovered-field case is prospective rather than
  present-tense: tool-map.md:37-40 documents the four write tools' free-text fields as exactly the six
  `_DEFANG_FIELDS` covers, so the gap bites on a remote-added or renamed tool/field, not on today's
  surface. The finding's own text already frames the allowlist as the defect; severity unchanged.
- PRAX-2026-08-19-009 — the failopen-counter and per-block status-marker promise sits at
  input-canonicalizer.md:164, in a section the spec labels design intent rather than shipped core;
  the primary claim (stderr-only disclosure) is unaffected.
- PRAX-2026-08-19-011 — R-24's literal hash-pinning clause is satisfied by the shipped lockfile; the
  `partial` status rests on the rule's purpose clause ("so a fresh install resolves the same dependency
  tree") together with the missing `--locked`. Recorded so the remit owner can decide whether to split
  that clause in a future remit revision.

**Remit feedback for the remit owner: none.** The rule-level check examined R-18, R-21, R-31, R-42,
R-47 and R-63 as over-reach candidates and cleared all of them.
