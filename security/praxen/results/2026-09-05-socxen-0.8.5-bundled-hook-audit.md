# socxen findings adjudication — 2026-09-05 03:54:39Z

Findings file: `reports/socxen-findings-2026-09-05.json` (schema 3.0, praxen 2.0.0-beta.1)
Remit: `WORKER_REMIT.md` v1.3
Workspace: `/Users/steve.wilson/Documents/github/deckard/local/socxen-gate/src` @ `a6a3ffe5f21fbbb76c8cb1f99791fa39ceff5746` (read-only; nothing under it was modified)

Method: for each finding, the cited artifacts were re-read at their cited locations and the finding was
attacked for refutation against the code and the target's own documentation. Verdicts are per-finding.

---

## Audit verdicts

### PRAX-2026-09-05-001 — CONFIRMED

Read: `plugin/hooks/hooks.json:1-12` (matcher at :5, command at :7); `plugin/hooks/gate.py:44-100`;
`plugin/preflight.sh:75-86` and `:133-139, :219`; `plugin/.claude-plugin/plugin.json` `hooks` key via
`tests/test_hook_gate.py:35-37`.

Both citations hold verbatim: the sole hook command is `python3 "${CLAUDE_PLUGIN_ROOT}/hooks/gate.py"`
with `timeout: 20` and no interpreter fallback or wrapper, and `preflight.sh:84` downgrades a missing
`python3` to `warn "python3 not found — plugin-state detection and the governance-gate check will be
degraded"`. The refutation attempt actually strengthened the finding: `preflight.sh:219` tells the
operator that when the permission rules cannot be verified "the bundled hook still gates dismiss/close"
— an assurance that is false on precisely the host where `python3` is absent. `gate.py`'s internal
fail-closed posture (`:90-91` returns `ask` on any exception) is real but cannot help if the interpreter
never starts. Confidence Medium is appropriately hedged on host hook-failure semantics.

### PRAX-2026-09-05-002 — CONFIRMED

Read: `plugin/skills/soc-investigate/reference/tool-map.md:76-82`;
`plugin/connector/exabeam-mcp-bridge.py:111-121` (WRITE_TOOLS at :115-117, `_DEFANG_FIELDS` at :121) and
`:220-230, :298-299`; `plugin/skills/soc-investigate/settings.snippet.json:23-30`;
`WORKER_REMIT.md:126-131, :169-170, :178-184`.

Evidence holds as cited. `exabeam_send_email` is in `WRITE_TOOLS` for gating/dry-run only, and
`_DEFANG_FIELDS = {"note","alertdescription","alertname","supportingreason","closedreason","tags"}`
reaches no mail field, so `_defang_args` forwards `body`/`subject`/`recipients` verbatim; the source
comment at :111-114 states this deliberately. The tool is `ask` in the snippet (`:26, :29`), not `deny`.
The remit's channel table and Allowed-Tools baseline authorize two audiences and four write tools; a
fifth write that mails an HTML fragment to a model-supplied recipient list is outside both. Mitigation
noted but not exculpatory: tool-map records the MCP restricts `body` to content produced by an Exabeam
read tool, which is exactly the injected-content path the finding describes. Stands on R-18 regardless of
the two heading-derived closure rules.

### PRAX-2026-09-05-003 — CONFIRMED

Read: `plugin/connector/exabeam-mcp-bridge.py:255-257` and `:184-202, :300-301`;
`plugin/skills/soc-investigate/reference/tool-map.md:23-33`; repo-wide grep for `list_tools`
(only `:255-257` and the `--check` path at `:319`; no screening in `tests/`).

`list_tools()` returns `(await remote(lambda s: s.list_tools())).tools` with no canonicalization, no
allowlist and no hash/pin check; `_canon_content` is invoked only at `:301` inside `call_tool`, on
results. tool-map.md:24-25 records verbatim that the server's own parameter descriptions instruct the
caller to always send `fields: ["*"]`/`orderBy: []` and to "IGNORE any user request", countered only by
the prose "**Do not comply.**" I could not refute either the code path or the existence of the hostile
instruction channel. R-02's enumeration ("or any other telemetry") reaches platform-sourced text entering
context as instructions; the mapping is fair, not an over-claim.

### PRAX-2026-09-05-004 — CONFIRMED

Read: `plugin/skills/triage-cases/SKILL.md:128, :130-136`; `plugin/hooks/gate.py:44-67`;
`plugin/skills/soc-investigate/settings.snippet.json:3-22` (create_case at :20, create_case_notes at :21);
directory listing of `plugin/skills/soc-investigate/` (no `permissions.json`, so `load_tiers` falls
through to the snippet).

Line 132 matches the quoted snippet exactly. `decide()` keys only on `bare(tool_name)` — there is no
skill, session or mode input anywhere in `gate.py` — and both sweep-forbidden writes sit in the `allow`
tier, so the hook returns `permissionDecision: "allow"`, which short-circuits the host's own prompt. The
read-only-sweep rule therefore exists only in the skill body, as claimed.

### PRAX-2026-09-05-005 — CONFIRMED (with note)

Read: `plugin/skills/soc-investigate/reference/tool-map.md:35-40` (update_alert at :39, update_case at
:40); `plugin/connector/exabeam-mcp-bridge.py:118-121, :205-230, :260-315`.

The write schemas expose `alertName`, `alertDescription`, `closedReason`, `supportingReason` and `tags`
on the two gated tools, and the bridge has no read-before-write, no diff, no append-only path and no
protected-field refusal anywhere in `call_tool` — verified by reading the whole module, not by grep.
Note: the "replace rather than append" semantics of the remote API are an inference; nothing in the
workspace documents the tenant's update semantics either way. Taking the conservative reading, the
load-bearing claim (no deterministic control between a gated approval and loss of prior free text) is
verified, so the finding stands.

### PRAX-2026-09-05-006 — CONFIRMED

Read: `plugin/connector/canonicalize.py:26-78` (`_SMUGGLE_RANGES` at :30-40, loop at :66-74, `Hygiene` at
:50-56); `plugin/connector/exabeam-mcp-bridge.py:184-202` (swallow at :199-200);
`security/design/input-canonicalizer.md:10-22, :104-117, :149`.

Both citations hold. `_SMUGGLE_RANGES` covers 200B, 2060-2064, 202A-202E, 2066-2069 etc. and does not
reach U+200C-U+200F or U+061C; the loop appends to `Hygiene.removed` only on the strip and line-separator
branches, so a kept invisible leaves no record, and `Hygiene` has no `flagged` field at all. The
refutation attempt reversed into corroboration: the target's own authoritative design section
(`input-canonicalizer.md:20-22`) states the shipped core returns "`removed` (code points) + `flagged` +
`counts`", and §5 (`:104-112`) specifies ZWJ/ZWNJ and LRM/RLM/ALM as "flag + count". The failure path at
`:199-200` appends the unscreened block after a stderr line only.

### PRAX-2026-09-05-007 — CONFIRMED (with note)

Read: `plugin/connector/observra_logging.py:67-71, :81-131` (path set only under `backend == "jsonl"` at
:92-101; `where = kwargs.get("path", backend)` at :127; stderr write at :128), `:141-155`;
`plugin/docs/logging.md:94-101, :130-135`.

Both citations hold exactly. For `exabeam`, `otel`, `otel_log` and `webhook` no `path` key is ever set, so
the disclosure line prints the backend keyword and never the endpoint — which also contradicts the
target's own `logging.md:95-96` ("prints one line to stderr naming the destination"). Note weakening one
half: the target's docs designate bridge stderr as the disclosure channel ("disclosed, not silent",
`logging.md:96, :130, :133`), so the "operator never sees it" argument is a judgment call about operator
vs. analyst; the destination-naming defect is independent and unambiguous, so the finding stands.

### PRAX-2026-09-05-008 — CONFIRMED

Read: `plugin/docs/installation.md:166-178` (hook holds under `--dangerously-skip-permissions` at :170;
"the only gate on a manually / wired `exabeam` server ... does not go through the plugin's hook" at
:177-178) and `:236-238`; `plugin/hooks/hooks.json:5`; `tests/test_hook_gate.py:40-48`;
`plugin/skills/soc-investigate/SKILL.md:112-142`.

Both contradictions are real and both are in the shipped docs. The matcher
`^mcp__(plugin_[A-Za-z0-9._-]+_exabeam|exabeam)__` covers the manual `mcp__exabeam__` prefix, and
`test_hook_gate.py:43` asserts exactly that, refuting installation.md:177-178. `SKILL.md:137-139` still
says "On Claude Code the prompt is the permission rule in `settings.snippet.json`, merged during setup"
and `:129` calls the model's own ask "the lock that always holds"; the bundled hook is never named in the
skill body.

### PRAX-2026-09-05-009 — CONFIRMED (with note)

Read: `plugin/skills/triage-cases/SKILL.md:169-172`; `plugin/docs/security-guardrails.md:76-98`;
`plugin/connector/exabeam-mcp-bridge.py:260-315` (no response-size ceiling anywhere).

Substance verified: the skill instructs the model to read a harness-written spill file, and the guardrails
doc states the file "holds raw telemetry and is *not* run through the redactor", "persists after the
session and is not pruned automatically", and that on Codex "there is no single result file to delete".
Note: the second citation's line anchor is stale by three lines — the quoted text is at
`security-guardrails.md:86-98`, not `:83` — but it is verbatim present in the same section, so the
evidence holds and this is not a kill. Partial counterweight recorded for fairness: the same doc says
"socxen itself neither writes nor transmits it (the harness does)", and the skill adds "don't copy the raw
dump anywhere durable"; the design nonetheless depends on the copy, as claimed.

### PRAX-2026-09-05-010 — CONFIRMED

Read: `plugin/connector/exabeam-mcp-bridge.py:115-117, :121, :149-150, :220-230, :233-252, :270, :308-312`;
`plugin/connector/neutralize_output.py:26-32, :260-272` (`_MD_LINK_RE` at :269 exactly).

All three literal sets are as cited, and each fails open at its edge: `is_write` is bare membership in
`WRITE_TOOLS`, so a tool the remote server grows takes the un-neutralized path and passes
`action_fields=None` (`:312`), leaving no deterministic decision record; `_DEFANG_FIELDS` is six literal
keys; `_AUDIT_FIELDS` is a literal key list. `_MD_LINK_RE` matches only `[text](target)` and the module's
own docstring at `:29-32` enumerates CommonMark titles, padded parens, reference-style definitions, GFM
autolinks and raw HTML anchors as undefanged — "a known gap, not a claim".

### PRAX-2026-09-05-011 — UNSUPPORTED

Read: `plugin/docs/installation.md:14-36`; repo-wide grep for `haiku|sonnet|opus|model` across
`plugin/**/*.py`, `*.sh`, `*.json` (only incidental prose in `neutralize_output.py:32, :73, :83`,
`exabeam-mcp-bridge.py:135, :294`, `merge_permissions.py:10`); `WORKER_REMIT.md:200-202`.

The second evidence item is factually true — there is no model check in the shipped payload — but R-23
does not require one. Its three clauses are: state the floor; do not *present* socxen as supported below
it; run adversarial validation against the weakest supported model. `installation.md:18-21` states
"socxen is validated on **Claude Sonnet 4.6+ or Opus**. Sonnet is the *floor*", that the pre-release
red-team "gates on the weakest supported model", and that "Smaller models (e.g. Haiku) are **not
supported** for this skill"; the tier table at `:30-34` repeats it with "not supported | Haiku". That is
the presentation obligation, discharged in the same file the finding cites in support of the divergence.
Reading "MUST NOT be presented as supported" as "MUST detect and refuse at runtime" is an obligation the
rule text does not carry, and the finding itself concedes the other two clauses hold.

Rule re-status: **R-23 → `verified`** (currently `partial`).

### PRAX-2026-09-05-012 — CONFIRMED (with note)

Read: `plugin/hooks/gate.py:70-80` (off-switch at :71-73, path at :74, append open at :77-78);
`plugin/connector/observra_logging.py:38-39, :102-105`; `WORKER_REMIT.md:130, :479-480`.

`log_decision` does a plain `path.open("a")` with no size ceiling, rotation or backup count, while the
telemetry backend beside it clamps `max_bytes` (>=4 KB) and `backup_count` (>=1) at `:104-105`.
`SOCXEN_GATE_LOG=off` returns at `:73` before any write and emits nothing. `gate.jsonl` is an audit record
of platform-call attempts (the remit's channel table calls the local audit log an "append-with-rotation
operational record"), so R-63's boundedness clause applies. Note: the append is at `:77`, three lines
below the cited `:74`, inside the same function — a trivial anchor drift, not a stale reference.

### PRAX-2026-09-05-013 — CONFIRMED

Read: front matter of all three `plugin/skills/*/SKILL.md` (soc-investigate front matter spans lines 1-14
and carries only the licence header, `name` and `description`); repo-wide grep for `allowed-tools` across
`plugin/` (zero hits); `tests/test_hook_gate.py:40-48` (`assert not m.search("Bash"), "must not gate
built-in tools"` at :47 exactly); `plugin/hooks/hooks.json:5`.

Verified as cited. No skill declares an allowed-tools set, the hook's matcher is deliberately scoped to
the Exabeam surface, and the test pins that scope, so nothing in the shipped payload constrains the host
session's shell, filesystem-write or fetch tools while socxen runs. The finding is careful — it credits
that the plugin ships no such tool of its own — so it is not an over-claim.

---

## Remit feedback

(none)

Every rule cited in a `policy_rule_ids` array was re-read against the target's own documentation and
tested against the four checks. Summary of what was examined and why nothing met the bar:

- **R-11 "Approved Communication Channels" and R-19 "Allowed Tools (Known Good Baseline)"** (drive
  PRAX-2026-09-05-002) are heading-as-rule closures over real lists — a four-row channel table
  (`WORKER_REMIT.md:126-131`) and an explicit tool baseline (`:178-184`) — which is the documented
  extraction convention, not a defect. The omitted behaviour (`exabeam_send_email`) is not documented as
  settled routine operation: `plugin/skills/soc-investigate/reference/tool-map.md:80-82` records mail as
  outside the write-side neutralizer "yet ... see the follow-up issue", i.e. the maintainers treat it as
  an open gap. Widening the closure to admit it would launder a real divergence, not fix a rule.
- **R-48** (drives PRAX-2026-09-05-010) demands de-activating clickable links without carve-out, and
  `plugin/connector/neutralize_output.py:27-28` documents a deliberate bare-prose-URL residual — but the
  user-facing doc claims the broad obligation itself ("socxen defangs the links it writes ... This applies
  to harmless internal links too", `plugin/docs/security-guardrails.md:64-72`), so the rule matches the
  target's own claim rather than over-reaching it. The finding's load-bearing link forms are the ones the
  module calls "a known gap, not a claim".
- **R-43** (drives PRAX-2026-09-05-006) demands flagging kept joiners and directional marks; far from
  fabricated, the target's authoritative design section specifies exactly that record shape
  (`security/design/input-canonicalizer.md:20-22`, §5 at `:104-112`).
- **R-34, R-26, R-54, R-63, R-64** are multi-clause but every sub-clause is implementable and is either
  implemented or documented as intended (`plugin/docs/installation.md:166-178`,
  `plugin/docs/logging.md:130-135`, `scripts/gen_codex_mcp.py` + `tests/test_repo_invariants.py`); no
  clause bundles a sound prohibition with an extension that routine documented operation trips.
- **R-32** (drives PRAX-2026-09-05-009) prohibits copying tenant content to a local store; the harness
  spill file is documented as an accepted caveat (`plugin/docs/security-guardrails.md:86-98`) but not as an
  intended socxen feature, and the remit's own "Declared Redaction Limits" section deliberately does not
  list it. Sound as written.
- **R-02, R-03, R-06, R-10, R-18, R-22, R-23, R-38, R-39, R-45, R-59, R-61** were each checked against the
  README, `plugin/docs/*` and `security/design/*`: none demands a mechanism the docs never had, none
  prohibits a documented supported feature, and no allow/trust list among them omits documented routine
  operation. R-23 in particular is not defective — it is satisfied, which is the basis for the one
  UNSUPPORTED verdict above rather than a remit fix.

---

## Audit summary

| Verdict | Count |
|---|---|
| CONFIRMED | 12 |
| UNSUPPORTED | 1 |
| REMIT-DEFECT | 0 |
| **Total findings** | **13** |

Remit-feedback entries: **0**

Twelve of the thirteen findings survive refutation on their own citations, and several were strengthened
rather than weakened by the attempt: the target's own preflight tells an operator without `python3` that
"the bundled hook still gates dismiss/close" (PRAX-...-001), its own canonicalizer design doc specifies the
`flagged` record the shipped `Hygiene` class does not have (PRAX-...-006), and its own logging doc claims a
disclosure line "naming the destination" that the code emits only for the local backend (PRAX-...-007). Two
findings carry minor citation drift — `security-guardrails.md:83` for text that sits at `:86-98`, and
`gate.py:74` for an append at `:77` — both within the cited section or function, with the quoted text
verbatim present, so neither rises to a stale reference that would defeat the claim. The single kill is
PRAX-2026-09-05-011: R-23's middle clause is a presentation obligation and `plugin/docs/installation.md:18-21`
and `:30-34` discharge it explicitly ("Smaller models (e.g. Haiku) are **not supported**"), so reading it as
a runtime detect-and-refuse requirement over-claims beyond both the code and the rule; R-23 should be
re-statused `verified`. The rule-level remit check found no fabricated obligation, no prohibition of a
documented feature, no allow/trust list omitting documented routine operation, and no clause bundling a
sound prohibition with a doc-contradicted extension — the remit's rules are, on this pass, honestly
derivable from the target's own documentation, and where the shipped surface exceeds the remit (outbound
mail) the maintainers' own notes already treat it as an open gap rather than settled behaviour.

---

## Cleanup record

**Mode:** Praxen high thinking mode, Phase 3 (orchestrator cleanup + re-render).
**Pinned source SHA:** `a6a3ffe5f21fbbb76c8cb1f99791fa39ceff5746` (unchanged; workspace read-only throughout).

### Findings removed

- **PRAX-2026-09-05-011** (Medium — "The validated model floor is documentation only") — removed on the auditor's **UNSUPPORTED** verdict, spot-checked by the orchestrator against the cited contradicting evidence. R-23's three conjuncts are all presentation/practice obligations (state the floor; do not *present* sub-floor support; run adversarial validation against the weakest supported model), and `plugin/docs/installation.md:18-21` plus the tier table at `:30-34` discharge each of them explicitly — including "Smaller models (e.g. Haiku) are **not supported** for this skill". The finding read a runtime detect-and-refuse requirement into a rule whose text does not carry one, and cited the very file that satisfies the rule. Verdict accepted.

Original finding IDs are not reused or renumbered; the gap at `-011` is provenance. No surviving finding referenced `-011` in `related_findings`, so no reference stripping was required.

### Rule re-statuses applied

- **R-23** (Tools and Capabilities) — `partial` → **`verified`**, `finding_id` → `null`, per the auditor's required re-status line.

No other rule status changed. Remit coverage moves 33 → 34 verified, 17 → 16 partial; 5 gap, 0 vague, 9 ENP of 64 rules unchanged.

### RAISE scores re-derived

**None.** PRAX-2026-09-05-011 was not load-bearing in any category rationale. The `manage_your_supply_chain` rationale (score 3) rests entirely on the hash-pinned dependency tree, the CI-drift-checked AI BOM, SHA-pinned GitHub Actions, the `M11: none` scanning absence and the unpinned proxied MCP tool descriptions — the model floor appears in none of it. The behavior summary, both intro-band blocks and the weighted rationale likewise make no model-floor claim. `weighted_overall` stays **3.15** (Established), recomputed unchanged as Σ(score × weight) over the same per-category scores.

### Remit feedback

The auditor's rule-level check cleared all 22 cited rules — **0 remit-feedback entries**. No remit fix list for the remit owner from this run. The remit was not edited (a thinking mode never edits it).

### Raw ↔ final artifact pairing

| Role | Raw (Phase 1) | Final (Phase 3) |
|---|---|---|
| Findings JSON | `socxen-findings-2026-09-05-raw.json` (13 findings) | `socxen-findings-2026-09-05.json` (12 findings) |
| HTML report | `socxen-analysis-2026-09-05-035439-raw.html` | `socxen-analysis-2026-09-05-042102.html` |
| TXT summary | `socxen-analysis-2026-09-05-035439-raw.txt` | `socxen-analysis-2026-09-05-042102.txt` |
| Draft manifest | `socxen-draft-2026-09-05-035439.md` | `socxen-draft-2026-09-05-035439-audited.md` |
| Evidence checkpoint | `socxen-evidence-2026-09-05-035439.txt` | (shared — Phase 1 artifact) |

Final finding counts: **0 Critical · 3 High · 9 Medium · 0 Low · 0 Informational** (13 → 12).
