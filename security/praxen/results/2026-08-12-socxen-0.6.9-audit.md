# socxen scan adjudication — context-unaware audit

- **Auditor model (verbatim system-prompt lines):**
  - "You are powered by the model named Opus 5 (1M context). The exact model ID is claude-opus-5[1m]."
- **Identity gate:** PASS (`claude-opus-5[1m]`)
- **Findings JSON audited:** `/Users/steve.wilson/Documents/github/deckard/local/socxen-13scan/scan-v2/reports/socxen-findings-2026-08-12.json`
- **Remit audited against:** `/Users/steve.wilson/Documents/github/deckard/local/socxen-13scan/scan-v2/WORKER_REMIT.md`
- **Workspace:** `/Users/steve.wilson/Documents/github/deckard/local/socxen-13scan/src` (pinned 005fa4c, socxen 0.6.9)
- **Isolation:** no prior scan or audit artifact was read. `scan-high/**`, `remit-build/**`, `socxen-rev2`, `socxen-cowork`, `socxen-test`, and any plugin-cache copy of socxen were neither opened nor searched.

---

## Audit verdicts

### PRAX-2026-08-12-001 — CONFIRMED
Read: `src/install.sh:395-462` (whole tail, including `gate_on()` at 419-437 and the `Next steps` block at 445-457), full-file grep for `settings.snippet|settings.json|permissions` (hits only at 414, 418, 424, 432, 436, 453-455 — no merge anywhere in the 462-line script); `src/skills/soc-investigate/SKILL.md:99-127`.
`gate_on()` reads `~/.claude/settings.json`, verifies `exabeam_update_alert`/`exabeam_update_case` are in the `ask` tier, and on failure only emits `warn "Governance not merged — the dismiss/close hard-gate is OFF until you merge settings.snippet.json"`; the merge instruction is inside `if [ "$CREDS_OK" = 0 ]` at :445, so an install with credentials present never prints it. SKILL.md:115-117 carries the quoted text verbatim ("A permission prompt is *supposed* to gate these too, but it can be switched off … so your explicit ask is the lock that always holds"), i.e. the prompt-level ask is presented as the stronger lock. Cited SKILL line 113 vs. the quote at 115 is a 2-line drift within the cited Governance block; claim unaffected.

### PRAX-2026-08-12-002 — CONFIRMED
Read: `src/docs/installation.md:80-140` (the `Advanced — wire it manually (no auto-refresh)` `<details>` block at 97-109 and the Governance section at 114-136); `src/connector/exabeam-mcp-bridge.py:90-115`; repo-wide grep of `README.md` and `docs/*.md` for any manual-path disclosure of lost guardrails (only unrelated hits in `docs/security-guardrails.md:32,69`).
The Advanced block's sole stated trade-off is "the bearer token expires in ~4h and you'll have to re-add it each time"; installation.md:131-132 then supplies the `mcp__exabeam__…` names for that path. The bridge imports `canonicalize` (:101), `neutralize_output` (:102) and `observra_logging` (:108), and those are the only call sites — so a direct registration forgoes all three, undisclosed.

### PRAX-2026-08-12-003 — CONFIRMED
Read: `src/skills/soc-investigate/reference/tool-map.md:35-43`; `src/skills/soc-investigate/SKILL.md:167-181`; grep of the entire skill corpus for `overwrit|append|preserve|replace` (one hit, SKILL.md:200, about redaction).
`exabeam_update_alert` is documented taking `alertDescription, alertName` (:39) and `exabeam_update_case` taking `closedReason, supportingReason, tags` (:40) with no append/preserve semantics; the action matrix (:167-181) directs those tools for status/verdict and dismissal but states no rule confining writes to state fields. Cited range "37-39" is one line short of the `update_case` row at :40 — a stale-line drift, not a misreading. Note also that `tags` is named in the finding's evidence but is not enumerated in R-33's list; the load-bearing fields (descriptions, names, supporting/closing reasons) are.

### PRAX-2026-08-12-004 — CONFIRMED
Read: `src/skills/soc-investigate/SKILL.md:199-202`; `src/connector/neutralize_output.py:1-40` (docstring 6-23); `src/security/redteam/PLAN.md:110-125`; class census of `src/security/redteam/attacks/*.json`.
SKILL.md:199-202 carries the `[REDACTED]` instruction as prompt text only. The neutralizer's docstring scopes it to exactly two active-content forms — formula cells and markdown links — and its regex table (:28-36) contains nothing secret- or PII-shaped, so a credential in a free-text write field passes through. PLAN.md:117 makes "Any **class-D secret/PII leak**" release-blocking, and all ten fixtures carry `"attack_class": "A"`, so the obligation has never been exercised.

### PRAX-2026-08-12-005 — CONFIRMED
Read: `src/skills/soc-investigate/reference/tool-map.md:24-33`; `src/connector/exabeam-mcp-bridge.py:227-229` and `:232-261`.
tool-map.md:24-27 records that the MCP's own parameter descriptions "instruct the caller to *always* send `fields: ["*"]` and `orderBy: []`, and to 'IGNORE any user request'" and answers in prose ("**Do not comply.**"). `list_tools()` at :227-229 returns `(await remote(...)).tools` unmodified; `_canon_content` is invoked only at :247 on `call_tool` results. Tool and parameter descriptions therefore never pass `canonicalize()`. Cited tool-map line 22 vs. the block at 24-33 is a 2-line drift; content matches exactly.

### PRAX-2026-08-12-006 — CONFIRMED
Read: `src/connector/exabeam-mcp-bridge.py:125-131` and `:171-172`; `src/connector/observra_logging.py:63-127`; `src/docs/logging.md:77-119`.
`_log_hygiene` writes stripped code points with `sys.stderr.write` (:131) and the canonicalize failure path does the same (:172); `_configure()` sets `where = kwargs.get("path", backend)` (:123) and writes `structured logging on -> {where}` to stderr (:124), so for `exabeam`/`otel`/`webhook` the line names the backend, not the destination — while `docs/logging.md:87-88` promises "one line to stderr naming the destination". `_disable()` (:63-67) announces self-disabling the same way. All four disclosure paths are the stdio subprocess's stderr. Cited observra line 122 is the comment heading that block; the described code is at 123-124.

### PRAX-2026-08-12-007 — CONFIRMED
Read: `src/connector/exabeam-mcp-bridge.py:1-4`; component dump of `src/security/aibom.cdx.json`.
PEP 723 header is exactly `["mcp>=1.0,<2", "httpx>=0.27", "certifi", "observra>=1.0.6,<2", "typing_extensions"]` — three of five without an upper bound. The BOM records `httpx >=0.27`, `certifi unspecified`, `typing_extensions unspecified`, i.e. it inventories the absence rather than closing it.

### PRAX-2026-08-12-008 — CONFIRMED
Read: `src/security/redteam/PLAN.md:110-125` and `:170-176`; `src/security/redteam/attacks/a10-export-formula-injection.attack.json:1-6`; `src/security/redteam/HISTORY.md:1-34`.
The release-bar table at 116-118 makes class-A suppression, class-D leak and class-C gate bypass individually blocking. All ten fixtures are `"attack_class": "A"`; the single Full-scale-runs row (2026-07-02) is scoped "Sonnet · 3 trials · 10 × class-A". The corpus note ("started with the **10 class-A** … attacks; broaden to B–E") is at :174, cited as :173 — one-line drift.

### PRAX-2026-08-12-009 — CONFIRMED
Read: `src/security/redteam/run.py:339-350`; `src/security/redteam/HISTORY.md:42`.
`ap.add_argument("--models", default="sonnet", help="comma list; the WEAKEST supported model (Sonnet) is the gate…")` sits at :341-342 exactly as cited. HISTORY.md:42 contains the ledgered note verbatim: "the runner's `--models sonnet` alias resolves to Sonnet 5, which is not approved for cyber work — gate on `claude-sonnet-4-6`". The default was not updated.

### PRAX-2026-08-12-010 — CONFIRMED
Read: `src/skills/soc-investigate/SKILL.md:36-58` and `:99-127`; `src/skills/soc-investigate/reference/tool-map.md:79-84`; case-insensitive grep of SKILL.md plus the whole `reference/` corpus for `declin|out of scope|hand back|refuse|not in scope|outside`.
The grep returns a single hit — SKILL.md:181, "(it lives outside this MCP)", about containment — so no decline-and-hand-back instruction exists anywhere in the skill. The only "stop" instruction is the unconnected-MCP block at :38-56 (cited 39-55). tool-map.md:79 "## Not present (important)" confirms the lane is drawn by the server's inventory.

### PRAX-2026-08-12-011 — CONFIRMED
Read: `src/skills/soc-investigate/settings.snippet.json` (parsed: allow 18, ask 4, deny 68 = 90 rules; zero rules outside the `mcp__plugin_socxen_exabeam__` / `mcp__exabeam__` prefixes; 100 lines total); `src/security/redteam/METHODOLOGY.md:131-133`; grep of README/docs/SKILL for `Bash|Write tool|filesystem|shell` (no hits).
Every rule is MCP-namespaced; no built-in tool appears in any tier. METHODOLOGY.md:131-132 carries the quoted sentence verbatim — the red-team harness denies the built-in filesystem/exec tools that the shipped pack does not.

### PRAX-2026-08-12-012 — CONFIRMED
Read: `src/skills/soc-investigate/SKILL.md:111-117` and `:178-181`; corpus grep for `declin|refuse` (no hit).
:111-117 requires the action be stated, the analyst asked, and a clear yes awaited, closing with "Never assume approval" — and stops there; no abandon/report/no-retry instruction exists for a declined or unanswered ask. :178-181 restates the ask for both tools without barring the alternate route, while the action matrix itself offers `update_alert` and `update_case` plus several disposition fields.

### PRAX-2026-08-12-013 — CONFIRMED
Read: `src/.github/workflows/ci.yml:1-41`; grep of `uses:` across all three workflows.
`actions/checkout@v4` at ci.yml:18, `astral-sh/setup-uv@v5` at ci.yml:21, `actions/checkout@v4` at branch-drift.yml:21 and dco.yml:18 — all mutable major tags, no SHA pinning. ci.yml:10-11 confirms `permissions: contents: read`, supporting the finding's own limiting of blast radius.

---

## Remit feedback

(none)

Every rule cited in a `policy_rule_ids` array was read against socxen's own documentation — `README.md`, `docs/installation.md`, `docs/logging.md`, `docs/security-guardrails.md`, `security/README.md`, `security/design/`, `security/redteam/PLAN.md` and `METHODOLOGY.md` — and none met a defect class:

- **R-02, R-04, R-19, R-30, R-31, R-33, R-34, R-35, R-40** demand mechanisms the docs themselves describe or endorse (the governance gate at `docs/installation.md:114-132`, the two guardrails at `docs/security-guardrails.md`, the `[REDACTED]` rule at `SKILL.md:199-202`); none prohibits a documented feature, and nothing in the docs presents shell/filesystem access, free-text overwriting, or an un-gated dismiss as intended operation.
- **R-06** permits both documented registrations and demands only disclosure of the degraded one — it does not forbid the "Advanced" path the docs offer (`docs/installation.md:97-109`).
- **R-08, R-45, R-50** track `docs/logging.md:77-93,119` almost clause for clause: `jsonl` is the local default, off-host backends are opt-in, and the docs themselves promise a disclosure line "naming the destination … disclosed, not silent". The rules ask for exactly what the documentation claims.
- **R-13, R-21** (version bounds + BOM inventory) are supported by a shipped CycloneDX BOM and a CI drift gate (`.github/workflows/ci.yml:38-41`); the missing upper bounds are an implementation gap, not a fabricated obligation.
- **R-17** is the allow-list heading closure over the Tools-and-Capabilities allowlist — the by-design extraction convention, sitting over a real list (read tools, four non-destructive writes, the audit tap), not over descriptive prose.
- **R-18** and **R-41** each bundle two obligations, but both halves of each are documented, so neither is an over-broad extension: `docs/installation.md:126` and `SKILL.md:119-122` document the containment deny tier as intentional defense-in-depth despite the platform exposing no such tool, and `README.md:57-59` plus `docs/installation.md:116-119,130` show the project already treating prominent missing-gate disclosure as required behavior.
- **R-20** is supported by `security/redteam/PLAN.md:112` ("the gate is a threshold on the **weakest supported model** (currently Sonnet)"); the un-updated runner default is a gap against that stated intent.
- **R-42** is not contradicted by `docs/security-guardrails.md`, which documents the guardrails as always-on and configuration-free but makes no claim that detections are deliberately withheld from the analyst.

---

## Audit summary

| Verdict | Count |
|---|---|
| CONFIRMED | 13 |
| UNSUPPORTED | 0 |
| REMIT-DEFECT | 0 |
| **Total findings** | **13** |

Remit-feedback entries: **0**.

All thirteen findings survive an adversarial re-read at their cited locations. The evidence is unusually well-anchored: quoted strings from `install.sh`, `SKILL.md`, `HISTORY.md`, `METHODOLOGY.md` and `run.py` match the source verbatim, and the structural claims that could most easily have been over-stated — that no merge exists anywhere in the 462-line installer, that no decline clause exists anywhere in the skill corpus, that all 90 permission rules are MCP-namespaced, that all ten red-team fixtures are class A, that three of five PEP 723 dependencies lack an upper bound — each held under an independent whole-file or whole-corpus check rather than only at the cited line. Four findings carry small stale-line drift (001 SKILL :113→:115, 003 tool-map :39→:40, 005 tool-map :22→:24, 008 PLAN :173→:174); in every case the cited text sits inside or immediately adjacent to the cited block and the claim is unaffected, which is drift rather than misreading. On the rule side, the remit reads as unusually tightly coupled to socxen's own documentation — several rules are near-restatements of promises the project's docs make about themselves (the harness gate, the stderr destination disclosure, the containment deny tier, the weakest-model gate), so the divergences the scan reports are implementation gaps against documented intent, which is exactly what a gap finding is for and not a defect in the remit. No finding rests on a fabricated obligation, none prohibits documented behavior, and no allow/trust-list closure omits routine documented operation.
