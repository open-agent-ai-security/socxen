{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "^mcp__(plugin_[A-Za-z0-9._-]+_exabeam|exabeam)__",
        "hooks": [
          { "type": "command", "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/gate.py\" || exit 2", "timeout": 20 }
        ]
      }
    ]
  }
}
